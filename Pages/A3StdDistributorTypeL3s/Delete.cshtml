@page "{id:guid}"
@model ControlCenterApp.Pages.A3StdDistributorTypeL3s.DeleteModel
@{
    ViewData["Title"] = "Delete Distributor Type L3";
}

<h1>Delete Distributor Type L3</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>@Model.Item.DistributorTypeL3</h4>
    <dl class="row">
        <dt class="col-sm-2">Description</dt>
        <dd class="col-sm-10">@Model.Item.ObjDescription</dd>
    </dl>
    <form method="post">
        <input type="hidden" asp-for="Item.ObjId" />
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-page="Index" class="btn btn-secondary">Cancel</a>
    </form>
</div>
