@page
@model ControlCenterApp.Pages.A3StdSaleunitRegions.IndexModel
@{
    ViewData["Title"] = "Saleunit Regions";
}

<h1>Saleunit Regions</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Region</th>
            <th>Description</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Items)
{
        <tr>
            <td>@item.SaleunitRegion</td>
            <td>@item.ObjDescription</td>
            <td>
                <a asp-page="Edit" asp-route-id="@item.ObjId">Edit</a> |
                <a asp-page="Delete" asp-route-id="@item.ObjId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
